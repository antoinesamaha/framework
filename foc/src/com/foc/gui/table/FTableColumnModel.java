package com.foc.gui.table;

import javax.swing.table.DefaultTableColumnModel;

@SuppressWarnings("serial")
public class FTableColumnModel extends DefaultTableColumnModel {

	//private int fTotalColumnWidth = -1;
	
	/*
	@Override
  protected void recalcWidthCache() {
  	Enumeration enumeration = getColumns();
  	totalColumnWidth = 0;
  	while (enumeration.hasMoreElements()) {
  	  totalColumnWidth += (tableCol).getWidth();
  	}
  }
  */
/*
	@Override
	public int getTotalColumnWidth() {
		if(fTotalColumnWidth > 0){
			return fTotalColumnWidth;
		}else{
			return super.getTotalColumnWidth();
		}
	}

	public void setTotalColumnWidth(int totalColumnWidth) {
		this.fTotalColumnWidth = totalColumnWidth;
	}
	*/
}
